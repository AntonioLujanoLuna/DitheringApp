{
    "buildCommand": "npm run vercel-build",
    "framework": "vite",
    "outputDirectory": "dist",
    "installCommand": "npm install",
    "routes": [
      { "handle": "filesystem" },
      { "src": "/(.*)", "dest": "/index.html" }
    ],
    "env": {
      "DISABLE_PWA": "true"
    }
  }